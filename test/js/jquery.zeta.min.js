jQuery.define(["jquery.epsilon", "jquery.theta"], function($){

	$.zeta = { zeta: true };

	$.fn.zeta = function(){
		return this.each(function(){
			$(this).html("Zeta <b></b>").find("b").theta();
		});
	};

});